// 배열에서 마지막 요소를 제거하고, 그 요소를 반환

const fruits = ["apple", "banana", "cherry"];

// 배열에서 마지막 요소 제거
const lastFruit = fruits.pop();

console.log(fruits);    // ["apple", "banana"]
console.log(lastFruit); // "cherry" (제거된 요소)
